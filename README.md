# tasksmanager
Tasks Manager Exercise

This app helps you manage youe tasks

To execute server run this:
`cd server`
`composer install`
`php artisan migrate'
`php artisan serve`

To execute client:
`cd client`
`npm install`
`npm run serve`
